name: Deploy Landing Page (deprecated)

on:
  workflow_dispatch:

jobs:
  redirect:
    name: Use Deploy Cloudflare (All)
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "::notice::Landing deploys now run via 'Deploy Cloudflare (All)'."
          echo "Trigger: Actions → Deploy Cloudflare (All) → Run workflow → target: landing"
          echo "Or push landing changes to master."

name: Deploy Docs (deprecated)

on:
  workflow_dispatch:

jobs:
  redirect:
    name: Use Deploy Cloudflare (All)
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "::notice::Docs deploys now run via 'Deploy Cloudflare (All)'."
          echo "Trigger: Actions → Deploy Cloudflare (All) → Run workflow → target: docs"
          echo "Or push docs changes to master."

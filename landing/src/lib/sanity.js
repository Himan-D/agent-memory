// Re-exports unified blog layer (Sanity CMS + static fallback)
export {
  SANITY_PROJECT_ID,
  urlFor,
  getCoverImageUrl,
  getFeaturedBlogs,
  getBlogs,
  getBlogBySlug,
  getBlogCategories,
  PortableText,
} from './blog.js'

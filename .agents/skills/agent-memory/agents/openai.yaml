interface:
  display_name: "Agent Memory"
  short_description: "Repo-specific patterns and workflows for agent-memory"
  default_prompt: "Use the agent-memory repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true